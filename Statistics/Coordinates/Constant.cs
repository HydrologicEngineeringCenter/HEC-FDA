using System;
using System.Collections.Generic;
using System.Text;

namespace Statistics
{
    //internal class Constant
    //{
    //    //TODO: Validate

    //    private readonly double _Value;

    //    public bool IsVariable => true;
    //    public Type OrdinateType => typeof(double);

    //    public Constant(double x)
    //    {
    //        _Value = x;
    //    }

    //    public double GetValue(double p = 0.50) => _Value;
    //    public string Print() => $"Constant(value: {_Value})";
    //    public bool Equals<T>(IOrdinate<T> parameter) => string.Compare(Print(), parameter.Print()) == 0 ? true : false;
    //}
}
