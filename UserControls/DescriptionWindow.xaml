<Window x:Class="DescriptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserControls"
        mc:Ignorable="d"
        Name="DW"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="DescriptionWindow" Width="400" Height="200" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>
        <TextBox  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextWrapping="Wrap" Text="{Binding UpdateSourceTrigger=PropertyChanged, Path=Description, ElementName=DW}" Margin="5,5,5,5" Name="TextBox1"/>
        <Button Content="OK" Grid.Row="1" Height="23" HorizontalAlignment="Right" Margin="5,5,60,5" Name="Cmdok" VerticalAlignment="Center" Width="50" />
        <Button Content="Close" Grid.Row="1" Height="23" HorizontalAlignment="Right" Margin="5,5,5,5" Name="CmdClose" VerticalAlignment="Center" Width="50" />

    </Grid>
</Window>
