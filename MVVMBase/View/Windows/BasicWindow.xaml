<local:Window x:Class="View.Windows.BasicWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Windows"
             xmlns:v="clr-namespace:View.NamedActionConverters"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="27"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Right" Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <v:NamedActionButton Grid.Column="0"  Height="18" Width="18" NamedAction="{Binding ErrorsAction}" IsEnabled="{Binding HasErrors, UpdateSourceTrigger=PropertyChanged}" ToolTip="Errors"/>
            <v:NamedActionButton Grid.Column="1" Height="18" Width="18" NamedAction="{Binding MessagesAction}" ToolTip="Messages"/>

        </Grid>
        <ContentControl Grid.Row="1" Content="{Binding}"/>
    </Grid>
</local:Window>
