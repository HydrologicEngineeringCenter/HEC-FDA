<UserControl x:Class="Fda.Plots.ExceedanceProbabilityAreaPlot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fda.Plots"
             xmlns:oxy="http://oxyplot.org/wpf"

             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <Border Name="Plot1Border" Grid.Row="0" Grid.Column="0" Margin="10" CornerRadius="1"  BorderBrush="Black" BorderThickness="2"  >
            <!--<Border.Background>
                <SolidColorBrush Color="AliceBlue"></SolidColorBrush>
            </Border.Background>-->
            <!--<Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="Red" Direction="235" Opacity=".5" RenderingBias="Quality" ShadowDepth="30" />
            </Border.Effect>-->
            <oxy:PlotView Name="OxyPlot1" Margin="0,0,0,0" Background="AliceBlue"  />
            
        </Border>
        <TextBlock Name="txt_AEP" Text="AEP: .25" Margin="20" FontSize="18"/>
    </Grid>
</UserControl>
