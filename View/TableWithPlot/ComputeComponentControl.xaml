<UserControl x:Class="View.TableWithPlot.ComputeComponentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.TableWithPlot"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="120">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Label Content="{Binding Path= Name}"  FontWeight="Bold"/>
            <Label Content="{Binding Path=Units}"/>
        </StackPanel>
        
        <StackPanel Orientation="Horizontal"  Grid.Row="1">
            <Label Content="Distribution Type: "/>
            <ComboBox Margin="5" ItemsSource="{Binding Path = Options}" Grid.Column="1" SelectedItem="{Binding Path=SelectedItem}" DisplayMemberPath="Name"/>
        </StackPanel>
        
        <local:FdaDataGridControl DataContext="{Binding Path =SelectedItem }" Grid.Row="2"/>
    </Grid>
</UserControl>
