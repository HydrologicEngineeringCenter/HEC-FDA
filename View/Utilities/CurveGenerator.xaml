<UserControl x:Class="View.Utilities.CurveGenerator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Utilities"
             xmlns:CA="clr-namespace:Consequences_Assist;assembly=Consequences_Assist"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <DataGrid Grid.Row="0" AutoGenerateColumns="True" ItemsSource="{Binding Curve.RowItems}" >
            <!--<DataGrid.Columns>
                <DataGridTextColumn Header="X" Binding="{Binding Path=X}" Width="*"/>
                <DataGridTextColumn Header="Y" Binding="{Binding Path=Y}" Width="*"/>
                <DataGridComboBoxColumn x:Name="DistTypeColumn" Header="Distribution Type" SelectedItemBinding="{Binding SelectedDistributionType, Mode=TwoWay}" Width="*"/>
                <DataGridComboBoxColumn x:Name="InterpTypeColumn" Header="Interpolation Type" SelectedItemBinding="{Binding SelectedInterpolationType, Mode=TwoWay}" Width="*"/>
            </DataGrid.Columns>-->
        </DataGrid>
    </Grid>
</UserControl>
