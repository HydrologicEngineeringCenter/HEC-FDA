<UserControl x:Class="HEC.FDA.View.Hydraulics.PathAndProbabilityTableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HEC.FDA.View.Hydraulics"
             xmlns:utilVM="clr-namespace:HEC.FDA.ViewModel.Utilities;assembly=HEC.FDA.ViewModel"
             xmlns:twp="clr-namespace:HEC.FDA.View.TableWithPlot"
             xmlns:griddeddata="clr-namespace:HEC.FDA.ViewModel.Hydraulics.GriddedData;assembly=HEC.FDA.ViewModel"
             xmlns:unsteadyhdf="clr-namespace:HEC.FDA.ViewModel.Hydraulics.UnsteadyHDF;assembly=HEC.FDA.ViewModel"
             d:DataContext="{d:DesignInstance Type=unsteadyhdf:UnsteadyHDFImporterVM}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
  <Grid>
    <twp:FdaDataGrid Grid.Row="4"
                     Grid.ColumnSpan="2"
                     ItemsSource="{Binding ListOfRows}"
                     x:Name="MyDataGrid"
                     AutoGenerateColumns="False"
                     CanUserSortColumns="false"
                     SelectionUnit="Cell"
                     RowsDeleted="MyDataGrid_RowsDeleted">
      <twp:FdaDataGrid.Columns>
        <DataGridTextColumn Header="Name"
                            Binding="{Binding Name}"
                            Width="*" />
        <DataGridTextColumn Header="Annual Exceedence Probability"
                            Binding="{Binding Probability}"
                            Width="*" />
        <DataGridTextColumn Header="Return Interval"
                            Binding="{Binding ReturnYear}"
                            Width="*" />
      </twp:FdaDataGrid.Columns>
    </twp:FdaDataGrid>
  </Grid>
</UserControl>
