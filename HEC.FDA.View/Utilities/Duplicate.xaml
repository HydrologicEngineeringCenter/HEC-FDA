<UserControl
  x:Class="HEC.FDA.View.Utilities.Duplicate"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:util="clr-namespace:HEC.FDA.View.Utilities"
  mc:Ignorable="d">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition
        Height="auto" />
      <RowDefinition
        Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition
        Width="*" />
    </Grid.ColumnDefinitions>

    <Grid
      Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition
          Width="auto" />
        <ColumnDefinition
          Width="auto" />
        <ColumnDefinition
          Width="auto" />
      </Grid.ColumnDefinitions>

      <Label
        Grid.Column="0"
        Content="Create"
        HorizontalAlignment="Left" />
      <ComboBox
        Grid.Column="1"
        Margin="4"
        ItemsSource="{Binding NumberOfDuplicateOptions}"
        SelectedItem="{Binding SelectedNumDuplicates, Mode=TwoWay}"
        SelectedIndex="0"
        HorizontalAlignment="Left" />
      <Label
        Grid.Column="2"
        Content="Duplicates."
        HorizontalAlignment="Left" />
    </Grid>
    
    <util:OKCloseControl
      Grid.Row="1"
      Grid.Column="0"
      Margin="1"
      VerticalAlignment="Bottom" />
  </Grid>
</UserControl>
